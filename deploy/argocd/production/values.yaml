# Auto-generated by deploy.sh from main.yml. Do not edit manually.
# Source: deploy/post-install/group_vars/all/main.yml
global:
  baseDomain: "etalbaas.com"
  environment: "production"
  imageRegistry: "ghcr.io/kou-etal"
  imagePullSecrets:
    - name: ghcr-pull-secret

services:
  common:
    replicaCount: 2

  tenantUser:
    replicaCount: 2
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi

  project:
    replicaCount: 2
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi

  function:
    replicaCount: 2
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        cpu: "1"
        memory: 512Mi

  event:
    replicaCount: 2
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi

  storage:
    enabled: true
    replicaCount: 2
    s3:
      endpoint: "https://ec040cd418edb15ac3f422104523fd93.r2.cloudflarestorage.com"
      bucket: "etalbaas-storage"
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi

certManager:
  issuer:
    email: "admin@etalbaas.com"

metadb:
  internal:
    instances: 3
    storage: 20Gi
    backup:
      enabled: false
      endpoint: "https://ec040cd418edb15ac3f422104523fd93.r2.cloudflarestorage.com"
      bucket: "etalbaas-backups"
      credentialsSecret: "r2-backup-creds"
      schedule: "0 2 * * *"
      retentionDays: 30

auth:
  jwt:
    jwksUri: "http://gotrue-dev.platform-system.svc:9999/.well-known/jwks.json"
    issuer: ""  # GoTrue v2.164.0 does not set iss claim; JWKS verification is sufficient
    audiences: []

gotrue:
  externalUrl: "https://auth.etalbaas.com"
  siteUrl: "https://dashboard.etalbaas.com"
  jwt:
    useAsymmetric: true
