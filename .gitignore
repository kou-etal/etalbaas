# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Environment 
.env
.env.local
.env*.local

# Dependencies
node_modules/
vendor/

# Build
dist/
/build/
out/

# Logs
*.log

# Learning log
learning-log.md
.claude/commands/mystudy.md

# Proto generated code (committed for Docker builds)
# proto/gen/

# Architecture (一旦除外)
docs/architecture.md
docs/e2e-test-plan.md

# Review notes
point.txt

# Deferred refactoring backlog
REFACTOR_BACKLOG.md

# Issue log
ISSUE_LOG.md

# Local tools
.helm-bin/

# Build artifacts
operator/dispatcher.exe

# Deploy config (NEVER commit — use .example templates)
deploy/post-install/.vault-password
deploy/post-install/group_vars/all/vault.yml
deploy/post-install/group_vars/all/main.yml
deploy/kubespray/inventory/production/hosts.yaml

# Plain-text secrets for kubeseal (NEVER commit)
deploy/scripts/secrets.env

# Production values — kept in git for ArgoCD (contains only domain/config, no secrets).
# deploy.sh regenerates this file with real values from main.yml.

# kubespray credentials (encryption keys, tokens — NEVER commit)
deploy/kubespray/inventory/production/credentials/

# kubespray custom group_vars (copied to inventory by deploy.sh)
deploy/kubespray/group_vars/

# kubespray clone (fetched via git clone -b v2.25.0)
deploy/kubespray/kubespray/

# Windows artifact
nul
.claude/
dashboard/nul
